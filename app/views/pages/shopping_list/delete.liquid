---
method: post
---

{% liquid
  graphql result = 'ingredient/delete', id: context.params.id

  if result.record.id
    redirect_to 'shopping_list/shopping-list'
  else
    echo 'Something went wrong'
    echo result.errors
  endif
%}