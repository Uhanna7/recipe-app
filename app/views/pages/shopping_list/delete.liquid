---
method: post
---

{% liquid
  graphql result = 'shopping_list/delete', id: context.params.id

  if result.record.id
    redirect_to 'shopping_list/shopping-list'
  else
    echo 'Failed to delete the shopping list'
    echo result.errors
  endif
%}