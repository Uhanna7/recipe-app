---
slug: session/create
method: post
---
{% liquid
  function res = 'modules/user/lib/commands/session/create', email: context.params.email, password: context.params.password
  if res.valid
    redirect_to '/shopping_list/shopping-list'
  else
    echo 'Invalid email or password'
  endif
%}