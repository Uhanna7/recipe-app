---
method: post
---


{% liquid
  graphql result = 'ingredient/create', name: context.params.name

  if result.record.id
    redirect_to '/'
  else
    echo 'Something went wrong'
    echo result.errors
  endif
%}