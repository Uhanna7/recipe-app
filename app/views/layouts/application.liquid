<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta content="authenticity_token" name="csrf-param">
    <meta content="{{ form_authenticity_token }}" name="csrf-token">

    <title>Recipe App</title>

    <link rel="stylesheet" href="{{ 'styles/app.css' | asset_url }}">
  </head>

  <body>

    <header>
      <h1>Recipe App</h1>
      <nav>
        <a href="/">Home</a>
        <a href="/shopping_list/shopping-list">Shopping List</a>
        <a href="/recipes/recipes">Recipes</a>
      </nav>
      <div class="button-container">
        {% if context.session %}
          <a href="../session/destroy" class="button">Logout</a>
        {% else %}
          <a href="../user/login" class="button">Login</a>
          <a href="../user/register" class="button">Sign Up</a>
        {% endif %}
      </div>
    </header>

    <main>
      {{ content_for_layout }}
    </main>

    <footer>
      <p>&copy; 2023 Hanna's and Marcell's Website. All rights reserved.</p>
    </footer>

    <script src="{{ 'scripts/app.js' | asset_url }}"></script>

  </body>
</html>