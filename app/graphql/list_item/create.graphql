mutation list_item_create($ingredient_id: String) {
  record: record_create(
    record: {
      table: "list-item"
      properties: [
        { name: "ingredient_id", value: $ingredient_id },
      ]
    }
  ) {
    id
    properties
  }
}