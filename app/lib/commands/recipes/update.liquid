{% liquid
  function object = 'lib/commands/recipes/update/build', object: object
  function object = 'lib/commands/recipes/update/check', object: object

  if object.valid
    graphql object = 'lib/commands/recipes/update', args: object
    hash_assign object['valid'] = true
  endif

  return object
%}